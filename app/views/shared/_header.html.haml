%nav.navbar.navbar-default{role: "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle{type: "button", data: {toggle: "collapse", target: "#nav-collapse"}}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: root_path} Home
    
    .collapse.navbar-collapse#nav-collapse
      %ul.nav.navbar-nav
        %li
          %a{href: users_path} Students
        %li
          %a{href: courses_path} Courses
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li
            %a{href: user_path(current_user.id)}= current_user.username
          %li
            %a{href: destroy_user_session_path, data: {method: "delete"}} Logout
        - else
          %li
            %a{href: new_user_registration_path} Register
          %li
            %a{href: new_user_session_path} Login